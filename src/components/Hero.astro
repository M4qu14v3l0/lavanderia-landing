<section class="hero-section">
    <div class="hero-bg">
        <h2>Lava como quieras, <span>cuando quieras</span></h2>
    </div>
</section>

<style>
    .hero-section {
        width: 100%;
        height: 100vh;
        background-image: url("/pictures/heroImage.svg");
        background-size: cover;
        background-repeat: no-repeat;
    }

    .hero-bg {
        height: 100%;
        background-color: #0a3143b2;

        display: flex;
        flex-direction: column;
        justify-content: flex-end;
    }

    .hero-bg h2 {
        display: flex;
        flex-direction: column;
        margin-bottom: 40px;
        color: white;
        font-size: clamp(18px, 6vw, 35px);
        padding: 0px 100px;
    }

    .hero-bg h2 span {
        font-size: clamp(18px, 6vw, 80px);
    }
</style>
